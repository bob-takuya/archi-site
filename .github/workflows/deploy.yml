name: Deploy to GitHub Pages (Disabled)

# This workflow is currently disabled
# We're using manual deployment instead

on:
  # Disable automatic triggers
  # push:
  #   branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "Workflow is disabled"
